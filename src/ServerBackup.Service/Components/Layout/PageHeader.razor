<header class="sb-pagehead">
    <h1>@Title</h1>
    <div class="sp"></div>
    @if (Actions is not null)
    {
        <div class="sb-row">@Actions</div>
    }
</header>

@code {
    [Parameter, EditorRequired]
    public string Title { get; set; } = "";

    [Parameter]
    public RenderFragment? Actions { get; set; }
}
